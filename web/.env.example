# Example environment variables for local development
# Copy to `.env.local` and fill values. Never commit secrets.

# Secret required to enable preview endpoints. Keep this secret private.
PREVIEW_SECRET=your-preview-secret-here

# WordPress GraphQL endpoint used by preview-proxy
NEXT_PUBLIC_WORDPRESS_GRAPHQL=https://example.com/graphql

# (Optional) If using basic auth for the WP app password
PREVIEW_USER=wp_username
PREVIEW_APP_PASSWORD=wp_application_password

# Node environment
NODE_ENV=development

# If you're using a local dev WordPress instance with a mkcert TLS certificate
# that Node doesn't trust, you can enable this to allow insecure upstream TLS
# only for local development (set to 'true'). Do NOT enable in production.
PREVIEW_ALLOW_INSECURE=false

# Alternatively, point to a CA bundle file containing the mkcert/root CA and
# set PREVIEW_CA_PATH to the file path. If provided, the proxy will attempt to
# use the given CA for upstream TLS verification.
# PREVIEW_CA_PATH=/path/to/rootCA.pem
